apiVersion: v1
kind: Service
metadata:
  name: mvc-app-service
spec:
  type: NodePort
  selector:
    app: mvc-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
